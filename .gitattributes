# Test fixtures must be byte-identical on every platform. Without this, Git
# checks out CRLF on Windows and any end-anchored pattern ($) stops matching,
# which is what the first windows-latest CI run caught.
*.md    text eol=lf
*.sh    text eol=lf
*.json  text eol=lf
*.yml   text eol=lf
